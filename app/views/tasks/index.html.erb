<h1>タスク一覧</h1>
    <%= link_to 'Signup', signup_path %>
    <%= link_to 'Login', new_task_path %>
<ul>
    <% @tasks.each do |t| %>
    <li><%= link_to t.id,t %> : <%= t.content %> &gt; <%= t.status %></li>
    <% end %>
</ul>

    <%= link_to '新規タスクの登録', new_task_path %>
